# A股量化交易系统 - 启动指南

## 📋 目录
1. [环境要求](#环境要求)
2. [安装步骤](#安装步骤)
3. [配置说明](#配置说明)
4. [启动后端](#启动后端)
5. [启动前端](#启动前端)
6. [验证运行](#验证运行)
7. [常见问题](#常见问题)

---

## 环境要求

### 必需软件
- **Python 3.8+** （推荐 3.10+）
- **Node.js 16+** （推荐 18+）
- **npm** 或 **yarn** 包管理器

### 检查环境
```bash
# 检查Python版本
python --version
# 或
python3 --version

# 检查Node.js版本
node --version

# 检查npm版本
npm --version
```

---

## 安装步骤

### 1. 设置虚拟环境（推荐方式）

**使用自动脚本（最简单）：**

**Windows:**
```bash
# 双击运行或命令行执行
setup_venv.bat
```

**Linux/Mac:**
```bash
chmod +x setup_venv.sh
./setup_venv.sh
```

脚本会自动：
- ✅ 创建Python虚拟环境（`venv`目录）
- ✅ 激活虚拟环境
- ✅ 升级pip
- ✅ 安装所有依赖（核心模块 + 后端API）

**手动设置虚拟环境：**
```bash
# 进入项目根目录
cd D:\CursorWorkspace\A股量化

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# 安装所有依赖（使用合并的依赖文件）
pip install -r requirements-all.txt
```

**不使用虚拟环境（不推荐）：**
```bash
# 安装核心模块依赖
pip install -r requirements.txt

# 安装后端API依赖
pip install -r backend/requirements.txt
```

### 2. 安装Node.js依赖（前端）

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install
# 或使用yarn
yarn install
```

---

## 配置说明

### 数据源配置

编辑 `config/config.yaml` 文件：

```yaml
data_source:
  default: akshare  # 推荐使用akshare（无需token）
  # 或使用tushare（需要token）
  tushare:
    token: "你的tushare_token"  # 从 https://tushare.pro/ 获取
```

**数据源选择：**
- **akshare**：免费，无需token，推荐新手使用
- **tushare**：需要注册获取token，数据更稳定

---

## 启动后端

### 方法1：使用启动脚本（推荐，自动使用虚拟环境）

**Windows:**
```bash
# 双击运行或命令行执行
start_backend.bat
```

**Linux/Mac:**
```bash
chmod +x start_backend.sh
./start_backend.sh
```

### 方法2：手动启动（使用虚拟环境）

```bash
# 激活虚拟环境
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# 启动后端
cd backend
python main.py
```

### 方法3：不使用虚拟环境

```bash
# 在项目根目录
cd backend
python main.py
```

### 方法2：使用uvicorn启动

```bash
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 方法3：使用启动脚本

**Windows:**
```bash
backend\start.bat
```

**Linux/Mac:**
```bash
chmod +x backend/start.sh
./backend/start.sh
```

### 验证后端启动成功

看到以下输出表示启动成功：
```
✓ API服务器启动完成
✓ 文档地址: http://localhost:8000/api/docs
INFO:     Uvicorn running on http://0.0.0.0:8000
```

**访问地址：**
- API文档：http://localhost:8000/api/docs
- 健康检查：http://localhost:8000/health
- API根路径：http://localhost:8000/

---

## 启动前端

### 启动开发服务器

```bash
# 进入前端目录
cd frontend

# 启动开发服务器
npm run dev
# 或
yarn dev
```

### 验证前端启动成功

看到以下输出表示启动成功：
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

**访问地址：**
- 前端应用：http://localhost:5173

---

## 验证运行

### 1. 检查后端API

打开浏览器访问：
- http://localhost:8000/api/docs
- 应该看到Swagger API文档界面

### 2. 检查前端界面

打开浏览器访问：
- http://localhost:5173
- 应该看到量化交易系统界面

### 3. 测试API接口

在Swagger文档页面（http://localhost:8000/api/docs）：
1. 点击 `GET /api/v1/strategies`
2. 点击 "Try it out"
3. 点击 "Execute"
4. 应该返回策略列表（可能为空数组）

---

## 完整启动流程（快速参考）

### 第一次启动

**方式1：使用脚本（最简单）**

**Windows:**
```bash
# 1. 设置虚拟环境（只需一次）
setup_venv.bat

# 2. 安装前端依赖（只需一次）
cd frontend
npm install
cd ..

# 3. 一键启动
start_all.bat
```

**Linux/Mac:**
```bash
# 1. 设置虚拟环境（只需一次）
chmod +x setup_venv.sh
./setup_venv.sh

# 2. 安装前端依赖（只需一次）
cd frontend
npm install
cd ..

# 3. 一键启动
chmod +x start_all.sh
./start_all.sh
```

**方式2：手动启动**

```bash
# 1. 设置虚拟环境
python -m venv venv
# Windows: venv\Scripts\activate
# Linux/Mac: source venv/bin/activate
pip install -r requirements-all.txt

# 2. 安装前端依赖
cd frontend
npm install
cd ..

# 3. 配置数据源（可选，默认使用akshare）
# 编辑 config/config.yaml

# 4. 启动后端（终端1，使用虚拟环境）
# Windows: venv\Scripts\activate
# Linux/Mac: source venv/bin/activate
cd backend
python main.py

# 5. 启动前端（终端2）
cd frontend
npm run dev
```

### 日常启动

**使用脚本（推荐）：**
```bash
# Windows
start_all.bat

# Linux/Mac
./start_all.sh
```

**手动启动：**
```bash
# 终端1：启动后端（记得激活虚拟环境）
# Windows: venv\Scripts\activate
# Linux/Mac: source venv/bin/activate
cd backend
python main.py

# 终端2：启动前端
cd frontend
npm run dev
```

---

## 常见问题

### 1. Python模块导入错误

**问题：** `ModuleNotFoundError: No module named 'xxx'`

**解决：**
```bash
# 确保安装了所有依赖
pip install -r requirements.txt
pip install -r backend/requirements.txt
```

### 2. 端口被占用

**问题：** `Address already in use`

**解决：**
- 后端端口8000被占用：修改 `backend/main.py` 中的端口号
- 前端端口5173被占用：Vite会自动使用下一个可用端口

### 3. 数据获取失败

**问题：** 无法获取股票数据

**解决：**
- 检查网络连接
- 如果使用tushare，检查token是否正确
- 尝试切换到akshare（无需token）

### 4. 前端无法连接后端

**问题：** 前端报错 `Network Error`

**解决：**
- 确保后端已启动（http://localhost:8000）
- 检查 `frontend/vite.config.ts` 中的代理配置
- 检查浏览器控制台的错误信息

### 5. CORS错误

**问题：** 浏览器控制台显示CORS错误

**解决：**
- 检查 `backend/main.py` 中的CORS配置
- 确保前端地址在允许列表中

### 6. 前端构建错误

**问题：** `npm install` 失败

**解决：**
```bash
# 清除缓存
npm cache clean --force

# 删除node_modules重新安装
rm -rf node_modules
npm install
```

---

## 开发模式 vs 生产模式

### 开发模式（当前配置）

- **后端**：自动重载（`--reload`），便于开发
- **前端**：热模块替换（HMR），实时更新

### 生产模式

**后端：**
```bash
uvicorn backend.main:app --host 0.0.0.0 --port 8000 --workers 4
```

**前端：**
```bash
cd frontend
npm run build
# 部署 dist 目录到Nginx等Web服务器
```

---

## 下一步

启动成功后，你可以：

1. **查看API文档**：http://localhost:8000/api/docs
2. **创建策略**：在前端界面创建你的第一个策略
3. **运行回测**：测试策略效果
4. **查看结果**：分析回测报告和图表

---

## 获取帮助

如果遇到问题：
1. 查看 `README.md` 和 `README_FULLSTACK.md`
2. 查看 `docs/architecture.md` 了解架构
3. 检查日志文件（如果配置了）
4. 查看浏览器控制台和终端错误信息

