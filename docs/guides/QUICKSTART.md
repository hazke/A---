# 快速启动指南

## 🚀 5分钟快速启动

> **重要说明：**  
> - Python 依赖可以通过虚拟环境管理（推荐）
> - Node.js 需要系统安装（类似 Python 解释器，不能放到虚拟环境）
> - 详细说明：[为什么需要安装 Node.js？](docs/why_install_nodejs.md)

### 步骤0：安装 Node.js（如果未安装）

如果看到 `'npm' is not recognized` 错误，需要先安装 Node.js：

1. 访问：https://nodejs.org/
2. 下载 LTS 版本（推荐 18.x 或 20.x）
3. 安装时确保勾选 "Add to PATH"
4. 重启终端后验证：`node --version` 和 `npm --version`

详细指南：[install_nodejs_guide.md](install_nodejs_guide.md)

### 步骤1：设置虚拟环境（推荐）

**所有平台（推荐）：**
```bash
python setup_venv.py
```

**或使用批处理文件（仅Windows）：**
```bash
# 双击运行或命令行执行
setup_venv.bat
```

**Linux/Mac批处理脚本：**
```bash
chmod +x setup_venv.sh
./setup_venv.sh
```

这将自动：
- ✅ 创建Python虚拟环境
- ✅ 安装所有Python依赖（核心模块 + 后端API）
- ✅ 配置好环境

**或者手动安装依赖（不使用虚拟环境）：**
```bash
# Python依赖
pip install -r requirements-all.txt

# Node.js依赖
cd frontend
npm install
cd ..
```

### 步骤2：启动系统

**方式1：一键启动（推荐，所有平台）**
```bash
python start_all.py
```

**或使用批处理文件：**
- Windows: `start_all.bat`（双击运行）
- Linux/Mac: `chmod +x start_all.sh && ./start_all.sh`

**方式2：分别启动**

**启动后端（所有平台）：**
```bash
python start_backend.py
```

**启动前端（所有平台）：**
```bash
python start_frontend.py
```

**启动前端（新终端）：**

```bash
cd frontend
npm run dev
```

✅ 看到 "✓ API服务器启动完成" 和 "Local: http://localhost:5173" 表示成功

### 步骤4：访问系统

- 🌐 前端界面：http://localhost:5173
- 📚 API文档：http://localhost:8000/api/docs

---

## ⚙️ 配置（可选）

如果需要使用tushare数据源，编辑 `config/config.yaml`：

```yaml
data_source:
  default: tushare
  tushare:
    token: "你的token"
```

**注意**：默认使用akshare，无需配置即可使用！

---

## 🐛 遇到问题？

1. **端口被占用**：修改 `backend/main.py` 中的端口
2. **模块缺失**：重新运行 `pip install -r requirements.txt`
3. **前端错误**：检查后端是否已启动

详细说明请查看 `启动指南.md`

